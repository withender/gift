# Cloudflare Pages 配置
# D1 请在 Dashboard → Pages 项目 → Settings → Functions 里绑定（Variable name: DB）
# 不要在这里写假的 database_id，否则部署会失败

name = "mygift"
compatibility_date = "2024-09-01"
pages_build_output_dir = "."
